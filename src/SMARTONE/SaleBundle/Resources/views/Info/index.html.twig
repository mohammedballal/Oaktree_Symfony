{% extends '::base.html.twig' %}


{% block body -%}
    <div class="padding">
        <div class="row">
            <div class="col-md-12">
                <div class="box">

                    <div class="box-header">
                        <h2>Info list</h2>
                    </div>

                    <table class="records_list table table-striped">
                        <thead>
                        <tr>
                            <th style="text-align: left;">For</th>
                            <th style="text-align: left;">Info</th>
                            <th style="text-align: right;">Action</th>
                        </tr>
                        </thead>
                        <tbody>
                        {% for entity in entities %}
                            <tr>
                                <td width="25%">{{ entity.numberText }}</td>
                                <td>{{ entity.text|raw }}</td>
                                <td align="right">
                                    <a class="btn btn-info btn-xs" href="{{ path('info_edit', { 'id': entity.id }) }}">Edit</a>
                                </td>
                            </tr>
                        {% endfor %}
                        </tbody>
                    </table>
                    <div class="dker p-a text-right">
                        <a ui-sref="app.widget" class="btn info" href="{{ path('info_new') }}">
                            New Info
                        </a>
                    </div>
                </div>

            </div>
        </div>
    </div>
{% endblock %}
