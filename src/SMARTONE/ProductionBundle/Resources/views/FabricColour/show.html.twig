{% extends '::base.html.twig' %}

{% block body -%}
    <h1>FabricColour</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Colourname</th>
                <td>{{ entity.colourName }}</td>
            </tr>
            <tr>
                <th>Sortorder</th>
                <td>{{ entity.sortOrder }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('fabriccolour') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('fabriccolour_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
