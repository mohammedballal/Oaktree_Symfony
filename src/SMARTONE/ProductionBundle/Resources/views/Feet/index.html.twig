{% extends '::base.html.twig' %}

{% block body -%}
<div class="padding">
    <div class="row">
        <div class="col-md-12">
            <div class="box">

                <div class="box-header">
                    <h2>Feet list</h2>
                </div>

    <table class="table table-striped table-condensed">
        <thead>
            <tr>
                <th>Id</th>
                <th>Feetname</th>
                <th>Feetsize</th>
                <th>Feetcolour</th>
                <th>Feettype</th>
                <th>Sortorder</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('feet_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.feetName }}</td>
                <td>{{ entity.feetSize }}</td>
                <td>{{ entity.feetColour }}</td>
                <td>{{ entity.feetType }}</td>
                <td>{{ entity.sortOrder }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('feet_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('feet_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

                    <div class="dker p-a text-right">
                    <a ui-sref="app.widget" class="btn info" href="{{ path('feet_new') }}">
                        New Feet
                    </a>
                </div>
                </div>

        </div>
    </div>
</div>

{% endblock %}
